{
	"name": "rehearsal-manager-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"start": "wrangler dev",
		"start:host": "wrangler dev --ip 0.0.0.0",
		"cf-typegen": "wrangler types",
		"init-db": "wrangler d1 execute rehearsal-manager-database --local --file=./schema.sql"
	},
	"devDependencies": {
		"typescript": "^5.8.3",
		"wrangler": "^4.25.0"
	},
	"dependencies": {}
}
